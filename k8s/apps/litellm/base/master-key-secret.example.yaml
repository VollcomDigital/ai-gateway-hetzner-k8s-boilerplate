# EXAMPLE ONLY — set explicitly for reproducible GitOps bootstrap.
# Alternatively omit and copy PROXY_MASTER_KEY from the Helm-generated secret after first install.
apiVersion: v1
kind: Secret
metadata:
  name: litellm-master-key
  namespace: litellm
type: Opaque
stringData:
  PROXY_MASTER_KEY: replace-with-strong-master-key
