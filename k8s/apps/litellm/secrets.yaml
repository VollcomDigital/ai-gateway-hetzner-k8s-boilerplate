apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: litellm
  labels:
    app.kubernetes.io/name: litellm
    app.kubernetes.io/part-of: ai-gateway
type: Opaque
stringData:
  # ──────────────────────────────────────────────────────────
  # PLACEHOLDER VALUES — replace before deploying or use a
  # sealed-secrets / external-secrets operator in production.
  # ──────────────────────────────────────────────────────────
  LITELLM_MASTER_KEY: "sk-CHANGE_ME_master_key"
  OPENAI_API_KEY: "sk-CHANGE_ME_openai"
  ANTHROPIC_API_KEY: "sk-ant-CHANGE_ME_anthropic"
