# EXAMPLE ONLY (do not commit real database credentials)
#
# Create the real Secret out-of-band via a secrets tool (recommended) or manually:
#   kubectl -n litellm create secret generic litellm-db-secret \
#     --from-literal=username='litellm' \
#     --from-literal=password='...' \
#     --from-literal=database='litellm' \
#     --from-literal=redis-password='...'
apiVersion: v1
kind: Secret
metadata:
  name: litellm-db-secret
  namespace: litellm
type: Opaque
stringData:
  username: litellm
  password: replace-with-strong-postgres-password
  database: litellm
  redis-password: replace-with-strong-redis-password

