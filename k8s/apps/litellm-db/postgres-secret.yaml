apiVersion: v1
kind: Secret
metadata:
  name: litellm-db-credentials
  namespace: litellm-db
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: ai-gateway
type: Opaque
stringData:
  # PLACEHOLDER — replace before deploying or use sealed-secrets
  POSTGRES_USER: litellm
  POSTGRES_PASSWORD: "CHANGE_ME_postgres_password"
  POSTGRES_DB: litellm
  DATABASE_URL: "postgresql://litellm:CHANGE_ME_postgres_password@postgres.litellm-db.svc.cluster.local:5432/litellm"
